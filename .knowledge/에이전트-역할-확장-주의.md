# 에이전트 역할 확장 주의사항

- 역할 목록은 런타임에 사용자 정의 역할이 붙으므로 개수를 상수로 가정하지 않는다.
- 선택 창 줄 수, 명령 자동완성 개수, Tab 순환 테스트는 고정 숫자 대신 실제 역할 목록 길이를 기준으로 검증한다.
- 사용자 정의 역할은 내장 역할 뒤에만 붙이고, 내장 역할과 같은 식별자는 무시한다. 외부 파일이 Builder 등 내장 역할을 조용히 대체해서는 안 된다.
- 사용자 정의 역할의 설치·표시·재시작 동작은 `docs/custom-agent-roles.md`를 함께 확인한다.
