# Devez Vibe 배포 버전 갱신 절차

## 업데이트 노트 작성과 사용자 검수

- 배포를 요청받으면 변경 사항과 이 문서에 쌓인 배포 대기 항목을 대조해 업데이트 노트 초안을 먼저 작성한다.
- 새 기능, 사용 방식 변경, 사용자에게 영향을 주는 오류 수정만 포함한다. 오탈자, 서식 정리, 내부 정리 등 사소한 변경은 제외한다.
- 항목마다 사용자에게 달라지는 점을 자연스러운 한국어 한 줄로 적는다. 기술 식별자만 원문을 유지하고 내부 구현 설명은 넣지 않는다.
- 초안을 사용자에게 보여주고 검수받는다. **명시적인 노트 승인 전에는 버전 변경과 게시를 진행하지 않는다.** 승인 대기 중에도 필요한 구현과 검증은 마친다.
- 승인받은 문구를 `src/update.rs`의 `RELEASE_NOTES`에 반영한다. 웰컴 화면의 `What's New`는 이 배열의 한국어 업데이트 노트를 표시한다.
- 배포 성공 후 배포 대기 항목을 비우고 승인된 노트를 아래 버전별 이력에 남긴다. 승인 이후 문구가 달라지면 다시 검수받는다.
- 참고: `C:/source/devezCode/배포.md`의 배포 요청 시 업데이트 노트 작성·사용자 확인 절차를 따른다. 이 프로젝트의 실제 게시 방식은 아래 npm 절차를 유지한다.

## 배포 대기 업데이트 노트 초안

- 없음.

## 승인된 버전별 업데이트 노트 이력

### 1.8.17 — 2026-09-10

사용자가 기존 화면 업데이트 노트 세 항목을 유지하고 모든 변경 사항을 함께 커밋·푸시·배포하도록 승인했다. 비동기 질문의 저장된 원문과 답변을 연결해 재개 후에도 질문·답변 상자와 화살표를 복원한다. 첫 검수에서 중단된 질문 턴의 정보가 제외되는 문제를 찾아 수정했다. 두 번째 검수에서 실제 Codex 연결을 종료하고 새 연결에서 thread/resume으로 직접 입력한 여러 줄 답변의 복원을 확인했다. 일반 시험 1,268개가 통과했으며 기본 제외 시험 13개 중 실제 비동기 질문 시험은 별도로 실행해 통과했다. 번호 선택·직접 입력·여러 질문·여러 줄·좁은 화면을 비교하고, 질문 정보가 없거나 불완전한 기록은 원문을 유지하도록 검증했다. 버전 갱신 후 웰컴 화면 시험 10개와 Release 빌드·배포 파일 구성 검증을 통과했으며 기존 사용하지 않는 코드 경고 3건은 남아 있다. 코드 커밋 `c84e942`를 원격 main에 푸시하고 npm 공개·설치·활성 버전 1.8.17을 확인했다. 공개 꾸러미 SHA-1 `db1ef24aad9d050bb1852f59bb76146ddce98253`은 배포 전 검증 값과 일치한다. Release 실행 파일·공개 설치본·활성 실행 파일 SHA-256 `cc98aa77ee526aa0cbdfbf4f55d9a37f09d47bdfdd2e5adb97c82fccc8f68928`이 일치하며 `dvz update`로 다음 실행 버전을 전환했다. 기존 미추적 Windows 캐시 폴더 `%SystemDrive%/`는 코드 변경이 아니므로 그대로 두었다.

### 1.8.16 — 2026-09-10

- 응답 중 링크 주소가 길게 나타났다 줄어드는 현상을 없애고, 링크가 완성되면 바로가기를 활성화합니다.
- 대기 요청이 다른 요청에 끼어들거나 미전송 초안의 이미지·자동완성 선택을 가져가는 문제를 수정합니다.
- Builder의 고정 분량 제한을 없애고, 중요한 근거를 유지하면서 간결하게 답하도록 합니다.

사용자가 기존 화면 업데이트 노트 세 항목을 유지하도록 승인했다. 링크 주소의 출력 지연·밝기 효과 제외, 원문과 클릭 대상 보존, 프로젝트 지식 문서 선별 안내를 포함한 모든 코드 변경을 함께 배포했다. 일반 시험 1,266개가 통과했으며 실제 모델 연결 시험 11개와 별도 터미널 화면 시험 2개는 제외했다. 버전 갱신 후 웰컴 화면 시험 10개, Release 빌드와 배포 파일 구성 검증을 통과했으며 기존 사용하지 않는 코드 경고 3건은 남아 있다. 코드 커밋 `ce530f8`을 원격 main에 푸시하고 npm 공개 버전과 설치·활성 버전 1.8.16을 확인했다. 공개 꾸러미 SHA-1 `67f92985abe003bf31860192bea20a0aa894e9c0`은 배포 전 검증 값과 일치한다. Release 실행 파일·공개 설치본·활성 실행 파일의 SHA-256 `b1d512406ade1f27e0537d9962ba064dcbc780de7f9f4d9fd2491d632ce3e4fa`가 일치한다. `dvz update`로 다음 실행 버전을 전환했다.

### 1.8.15 — 2026-09-09

- 응답 중 링크 주소가 길게 나타났다 줄어드는 현상을 없애고, 링크가 완성되면 바로가기를 활성화합니다.
- 대기 요청이 다른 요청에 끼어들거나 미전송 초안의 이미지·자동완성 선택을 가져가는 문제를 수정합니다.
- Builder의 고정 분량 제한을 없애고, 중요한 근거를 유지하면서 간결하게 답하도록 합니다.

사용자가 초안 1·3·4번만 화면 업데이트 노트에 반영하도록 승인했다. 나머지 링크 처리, ESC 거절·중단과 질문 기록 보존, 답변 입력 기록 분리, 로그인 영어 표시를 포함한 모든 코드 변경을 함께 배포했다. 삭제된 Builder 고정 제한을 기대하던 제공자별 시험을 현재 요구에 맞춰 수정했으며 일반 시험 1,262개가 통과했다. 실제 모델 연결 시험 11개와 별도 터미널 화면 시험 2개는 제외했다. 버전과 노트 갱신 후 웰컴 화면 시험 10개, Release 빌드와 패키지 구성 검증을 통과했으며 기존 사용하지 않는 코드 경고 3건이 남아 있다. 코드 커밋 `f257fb0`을 원격 main에 푸시하고 npm 공개 버전과 설치 후 실행 버전 1.8.15를 확인했다. 공개 설치본·활성 실행 파일·Release 실행 파일 SHA-256 `3acc2613e40655b58f7037c27a27f17b9aa0b16fc8bed77156275cf9b65f43ac`가 일치한다. `dvz update`로 다음 실행 버전을 전환했다.

### 1.8.14 — 2026-09-09

- 검토자 역할을 선택 목록과 탭 전환에 복원하고, 조사자와 목표 실행자 사이에 표시합니다.

이전 승인에 따라 화면 업데이트 노트 네 항목은 유지했다. 탭 전환 순서, 역할 선택 목록, 명령 자동완성과 검토자 선택을 포함한 일반 시험 1,242개를 통과했으며 실제 모델 연결 시험 11개와 별도 터미널 화면 시험 2개는 제외했다. Release 빌드와 배포 파일 구성 검증을 통과했으며 사용하지 않는 코드 경고 3건은 남아 있다. npm 공개 버전과 설치 후 실행 버전 1.8.14, 공개 설치본과 Release 실행 파일의 SHA-256 `5c457d72107118de2e72ac1d25aad121f1f54b280cb4ecb8dc8581522c07bce4` 일치를 확인하고 `dvz update`로 다음 실행 버전을 전환했다.

### 1.8.13 — 2026-09-09

이전 승인에 따라 화면 업데이트 노트 네 항목을 유지하고, 사용자가 요청한 원격 최신 커밋 `056c274`의 변경 사항을 모두 포함했다.

- 기본·역할 지침의 반복을 줄이고 검토 기준을 통일하며, 기존 백업과 사용자 설정을 보존하면서 기본 하위 지침을 갱신합니다.
- Builder의 일반 완료 보고는 공백 포함 200자 이하로 제한하고, 상세 분석·설명 요청과 선택·승인 답변은 분량 제한에서 제외합니다.
- 외부 터미널에서 한글을 입력할 때 마지막에 누른 키의 영문이 덧붙는 문제를 수정합니다.

일반 시험 1,242개와 Claude 연동·권한 자체 점검을 통과했다. 실제 모델 연결 시험 11개와 별도 Windows Terminal 화면 시험 2개는 제외했다. Release 빌드와 배포 파일 구성 검증을 통과했으며 사용하지 않는 코드 경고 3건이 남아 있다. npm 공개 버전과 설치 후 실행 버전 1.8.13, 공개 설치본과 Release 실행 파일의 SHA-256 `ec05165cf58d2d5a55fd7dd4dd0d0e31296bad602382f6d66f3a944f1a33c7e3` 일치를 확인했다. `dvz update`로 다음 실행 버전을 전환했다. 검토자 역할의 탭 표시 복원은 이번 원격 변경에 포함되지 않았으며 숨김 상태를 유지한다.

### 1.8.12 — 2026-09-09

사용자가 기존 화면 업데이트 노트 네 항목을 유지하는 배포를 승인했다. 아래 변경 사항을 모두 포함했다.

- 그림 출력과 글자 크기가 확인된 Windows Terminal에서는 얇은 반 줄 모서리를 픽셀로 보정합니다. DevezCode 내장 터미널의 모서리 보정은 별도 앱 수정이 필요합니다.
- Codex 질문 도구 지침에 선택지 항목명과 설명을 분리하고, 설명이 필요하지 않거나 지원되지 않으면 항목명만 전달하도록 추가합니다.
- 띄어쓰기 없는 목록 번호와 겹친 번호를 정리하고 질문 기본 제목·단계 표시를 제공자 간 통일합니다.
- 질문 상자 아래에 답변 상자를 들여쓰고, 상자 밖의 꺾인 화살표로 질문과 답변을 연결합니다.
- 질문·답변 상자의 테두리 옆 배경 틈을 없애고, 상자마다 위아래 여백 한 줄을 유지하며 두 상자 사이의 추가 빈 줄은 제거합니다.
- 답변 상자의 위쪽 여백은 아래 절반만, 아래쪽 여백은 위 절반만 배경을 표시합니다.
- 소요 시간을 질문 상자에 표시하고, 답변 상자의 반 줄 배경을 왼쪽 끝까지 연결하며 비동기 질문 원문이 본문에 중복으로 남지 않도록 합니다.
- 질문·답변 상자의 세로선과 연결 화살표를 질문을 띄운 모델 색으로 표시합니다.

기존 사용자 지시에 따라 실제 화면의 업데이트 노트 네 항목은 유지합니다. 비동기 질문이 제공하지 않는 설명과 복수 선택 정보를 임의로 생성하지 않으며, 해당 정보가 없는 질문끼리의 표시 일치와 입력 동작을 검증합니다.

일반 시험 1,239개와 Claude 연동·권한 자체 점검을 통과했다. 실제 모델 연결 시험 11개와 별도 Windows Terminal 화면 시험 2개는 제외했다. 버전 갱신 후 Release 빌드와 배포 파일 구성 검증을 통과했으며 사용하지 않는 코드 경고 3건이 남아 있다. npm 공개 버전과 설치 후 실행 버전 1.8.12, 공개 설치본과 Release 실행 파일의 SHA-256 `efcf15e1fd820fe3072b99a7a9231d29e11f5903c479b0a0187a9372d3ad68cf` 일치를 확인했다. `dvz update`로 다음 실행 버전을 전환했다.

### 1.8.11 — 2026-09-09

사용자 지시에 따라 기존 업데이트 노트 네 항목을 유지했다. Codex 비동기 질문 답변은 즉시 제출 시 질문·답변 원문을 표시하고, 중단 확인 후 제출 시 모델 전달용 안내까지 표시하여 Claude의 답변 화살표가 없었다. 두 경로 모두 Claude와 같은 답변 표시 함수를 사용하도록 통일했다. 번호 중복 방지 수정도 포함한다. 제공자별 동기·비동기 질문, 중단 전·후 선택, 직접 입력, 좁은 화면과 두 대화 배치의 표시 비교를 포함해 일반 시험 1,230개를 통과했으며 실제 모델 연결 시험 11개는 제외했다. npm 공개 버전과 설치 후 실행 버전 1.8.11, 공개 설치본·Release 실행 파일 SHA-256 `5b2c27ee5772ce3717f252309dfff096da7cfa6eb21531ec5f19a34f2ddbca6e` 일치를 확인했다. `dvz update`로 다음 실행 버전을 전환했다.

### 1.8.10 — 2026-09-09

사용자 지시에 따라 기존 업데이트 노트 네 항목을 유지했다. 선택창에서 모델이 붙인 목록 번호를 표시할 때만 제거하고 원문 답변과 버전·수량은 보존하도록 변경했다. Codex가 본문으로 질문할 때도 번호를 쓰도록 지침을 보강했다. 일반 시험 1,229개와 Claude 연동·권한 자체 검사를 통과했으며 실제 모델 연결 시험 11개는 제외했다. npm 공개 버전과 설치 후 실행 버전 1.8.10, 공개 설치본·Release 실행 파일 SHA-256 `aeb6e23924ea47d9407ffb6858ac0362f6c7a4faff161852b9fa08fd3005e1e2` 일치를 확인했다. `dvz update`로 다음 실행 버전을 전환했다. 본문 질문의 번호는 모델 지침으로 유도하며 모든 생성 응답의 준수를 보장하지 않는다.

### 1.8.9 — 2026-09-09

기존 사용자 지시에 따라 업데이트 노트 네 항목을 그대로 유지했다. 어두운 테마의 Builder 색상을 브랜치 표시와 동일하게 맞추고, 알 수 없는 명령 안내를 영문 한 줄과 테마 강조색으로 표시하도록 변경했다. 일반 시험 1,227개와 Claude 연동·권한 자체 검사를 통과했으며 실제 서비스 연결 시험 11개는 제외했다. npm 공개 버전과 설치 후 실행 버전 1.8.9, 공개·설치 실행 파일 SHA-256 `146f28c278b68799d04b2a1230f28af4831b59cbfef3fc355cf6146b4262d902` 일치를 확인했다. `dvz update`로 다음 실행 버전을 전환했다.

### 1.8.8 — 2026-09-09

사용자 지시에 따라 1.8.7 업데이트 노트 네 항목을 그대로 유지했다. 기본 역할 Builder를 프롬프트 하단에서 숨기고, 업데이트 노트의 긴 항목은 줄바꿈 대신 `...`로 표시하도록 변경했다. 일반 시험 1,226개와 Claude 연동·권한 자체 검사를 통과했으며 실제 서비스 연결 시험 11개는 제외했다. npm 공개 버전과 설치 후 실행 버전 1.8.8, 공개·설치 실행 파일 SHA-256 `3cdba29d4c48781dce620d4fbacf0033bc2ed19cdb0da36a3aa69fa331909bd5` 일치를 확인했다. `dvz update`로 다음 실행 버전을 전환했다.

### 1.8.7 — 2026-09-09

- 보낸 프롬프트에 처리 역할을 역할별 색상으로 표시하고, 완료 후 소요 시간을 함께 표시합니다.
- 질문 답변이 중복 알림이나 전송 실패로 사라지지 않도록 하고, 보내지 않은 초안의 첨부가 답변에 섞이지 않도록 수정했습니다.
- Claude는 자동 승인 검토를 사용하고, Codex는 전체 접근이 정책상 거절되면 허용되는 낮은 권한으로 전환합니다.
- 하위 에이전트의 작업 이름을 읽기 쉽게 표시하고 경과 시간을 함께 보여 줍니다.

사용자가 초안의 마지막 두 항목을 업데이트 노트에서 제외하도록 승인했다. 백그라운드 명령 추적·검증 지침과 기본 제공 스킬 중복 표시 수정도 코드 배포에는 포함했다. 일반 시험 1,226개와 Claude 연동·권한 자체 검사를 통과했으며 실제 서비스 연결 시험 11개는 이번 일반 시험에서 제외했다. Release 빌드, npm 공개 버전 1.8.7, 공개 꾸러미 SHA-1 `09488fbc2b1f7ba05a1cfe2f820ccc85378805c0`, 공개·설치 실행 파일 SHA-256 `c618abe9a83df28cdb69099ee5642b258ddb31a9b01ebcbeb4e9ae601291b1d6` 일치를 확인했다. `dvz update`로 다음 실행 버전을 전환했다.

### 1.8.6 — 2026-09-09

- 심층 조사를 위한 Researcher 역할을 추가했습니다.
- 선택지를 표시하고 답변을 기다리는 기능을 Codex 제공자에도 추가했습니다.

사용자가 이미 게시된 업데이트 노트를 그대로 재사용하도록 지정했다. 비동기 질문을 오류로 버리던 처리를 수정했으며, 일반 시험 1,211개와 실제 Astra 비동기 질문 복구 시험을 통과했다. npm 공개 버전·설치 후 실행 버전 1.8.6과 공개 실행 파일의 SHA-256 일치를 확인했다.

### 1.8.5 — 2026-09-09

- 심층 조사를 위한 Researcher 역할을 추가했습니다.
- 선택지를 표시하고 답변을 기다리는 기능을 Codex 제공자에도 추가했습니다.

사용자 지시에 따라 전수 재검증 후 수정 사항을 배포하고, 이전 업데이트 노트 두 항목을 그대로 유지했다.

### 1.8.4 — 2026-09-08

- 심층 조사를 위한 Researcher 역할을 추가했습니다.
- 선택지를 표시하고 답변을 기다리는 기능을 Codex 제공자에도 추가했습니다.

사용자가 두 항목만 다듬어 반영하고 검증·재검증 후 배포하도록 지정했다.

## 웰컴 UI 버전의 출처

- 웰컴 UI의 `vX.Y.Z` 표기는 `src/update.rs`의 `CURRENT_VERSION`을 사용한다.
- `CURRENT_VERSION`은 `Cargo.toml`의 `package.version`에서 컴파일 시점에 읽는다.
- 따라서 웰컴 UI 버전을 바꾸려면 `Cargo.toml`의 `version`을 변경하고 Release 빌드를 새로 해야 한다. 렌더러의 문구를 직접 수정하지 않는다.

## npm 배포 전 체크리스트

1. `git fetch`와 `git pull`로 원격 `main`을 먼저 병합한다. 로컬보다 npm 게시본이나 원격 작업이 앞선 상태에서 배포하면 최근 변경이 빠질 수 있다.
2. `npm view devez-vibe version`으로 이미 게시된 최신 버전을 확인하고 그보다 높은 새 버전을 정한다.
3. `Cargo.toml`과 `npm/package.json`의 `version`을 같은 새 버전으로 맞춘다.
4. `src/update.rs`의 `RELEASE_NOTES`를 사용자가 승인한 업데이트 노트로 갱신한다.
5. `RELEASE_NOTES`의 모든 항목은 자연스러운 한국어로 작성하고, 제품명·명령어·경로 등 기술 식별자만 원문을 유지한다.
6. Release 빌드 후 npm 패키지에 포함할 실행 파일을 교체한다.

```powershell
cargo build --release
Copy-Item target\release\dvz.exe npm\bin\dvz.exe -Force
Set-Location npm
npm pack --dry-run
npm publish --access public
```

7. 배포 버전과 설치 결과를 확인한다.

```powershell
npm view devez-vibe version
npm install -g devez-vibe@latest
dvz version
```

## 배포 요청 시 범위

- 사용자가 배포를 요청하면 그때까지의 모든 코드 변경 사항을 한 번에 묶어 하나의 새 버전으로 배포한다. 일부만 골라 내보내거나 나중으로 미루지 않는다.
- 배포한 내용은 커밋하고 원격에 푸시하는 데까지 진행한다. 커밋만 남기고 푸시를 미루지 않는다.
- 커밋 메시지는 `release X.Y.Z`로 시작하고, 이번 배포에 담긴 변경을 한국어로 적는다.

## 배포 요청 시 인증 방식

- 사용자가 배포를 요청하면 npm 토큰으로 인증해 배포한다. OTP 입력을 다시 묻지 않는다.
- 토큰은 이미 npm 로그인 상태로 준비되어 있다고 보고, `npm whoami`로 로그인 계정만 확인한 뒤 배포를 진행한다.
- 토큰 인증이 유효하지 않을 때만 사용자에게 알리고, 임의로 다른 인증 수단을 시도하지 않는다.

## 주의

- npm은 이미 게시된 버전을 다시 게시할 수 없다. `npm publish` 전에 `npm/package.json`의 버전이 새 값인지 확인한다.
- `dvz`는 전역 npm 설치본을 실행한다. 소스의 최신 Release 빌드는 `dvz-debug`로 실행해 확인한다.
