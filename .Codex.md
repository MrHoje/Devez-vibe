# 교훈

- 전각(한글) 글리프 깨짐: 방출 스트림이 VT 기준으로 완벽해도 ConPTY(WebView2+xterm.js 호스트)가 행 중간 절대 커서이동(CUP)을 재합성하며 글리프를 복제/밀림시킴. 전각 셀이 걸린 변경은 반드시 행 전체를 0열부터 순차 재인쇄할 것 (`emit_row_sequential`, v0.1.32). 스트림 레벨 수정(전체 리페인트 등)으로는 재발함 — 렌더링 깨짐 진단 시 VT 에뮬레이터 테스트로 스트림 정상 여부부터 확인.
- 이모지 폭 동기화: 코드포인트별 `UnicodeWidthChar` 합은 VS16·ZWJ·국기 같은 조합 폭과 다르므로 사용 금지. 렌더 프레임은 확장 그래핌 단위로 묶고 `UnicodeWidthStr`로 재며, 호스트 터미널도 그래핌 결합과 같은 폭을 쓰는지 실제 버퍼 셀로 검증할 것.
- 컴포저 전용 글리프 보정은 테두리 문자나 화면 하단 거리로 영역을 추측하지 않는다. dvz가 실제 컴포저 행을 전용 신호로 알리고, 호스트는 그 행만 교체해야 출력 아이콘 중복과 패널 변화 회귀를 막을 수 있다.
