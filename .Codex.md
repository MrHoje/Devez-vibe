# 교훈

- 전각(한글) 글리프 깨짐: 방출 스트림이 VT 기준으로 완벽해도 ConPTY(WebView2+xterm.js 호스트)가 행 중간 절대 커서이동(CUP)을 재합성하며 글리프를 복제/밀림시킴. 전각 셀이 걸린 변경은 반드시 행 전체를 0열부터 순차 재인쇄할 것 (`emit_row_sequential`, v0.1.32). 스트림 레벨 수정(전체 리페인트 등)으로는 재발함 — 렌더링 깨짐 진단 시 VT 에뮬레이터 테스트로 스트림 정상 여부부터 확인.
  - 그 순차 재인쇄에는 예외가 하나 있었다. 화면 범위가 그대로인 컴포저(입력) 행은 타이핑 중 캐럿이 행을 가로지르지 않도록 셀 diff를 유지하는데(`composer_rows_protected_from_sequential_repaint`), 선택지 창이 그 범위를 차지하면 항목 강조가 옮겨질 때마다 한글 항목 행이 행 중간 패치로 갱신돼 같은 깨짐이 났다. 선택지 창이 열린 프레임에서는 그 보호를 풀어 항목 행도 0열부터 재인쇄한다(`question_overlay_open` 인자).
  - 이 깨짐은 CLI·터미널 종류와 무관하다. 순정 Claude Code도 같은 증상이 나며, 그쪽 회피 스위치는 `CLAUDE_CODE_ALT_SCREEN_FULL_REPAINT=1`(DevezCode가 탭 세션에 이미 주입)이지만 dvz는 그 변수를 읽지 않으므로 dvz 화면은 자체 순차 재인쇄로만 막힌다.
  - 인코딩 문제로 오진하기 쉽다. UTF-8↔CP949·EUC-KR 등 어떤 왕복 변환도 이 결과를 만들지 못하고(손상 시 U+FFFD나 `?`가 남는다), 화면 글자를 복사해 붙여넣어 정상이면 데이터가 아니라 그리기 단계 문제다.

- 배포는 반드시 `git fetch`/`git pull`로 원격 main을 먼저 병합한 뒤 빌드·게시한다. npm 게시본이 로컬 커밋보다 앞서 있을 수 있어(예: 로컬 1.5.9 · npm 1.5.20), 병합 없이 게시하면 원격의 최근 작업이 빠진 패키지가 latest가 된다. 다음 버전 번호도 `npm view <pkg> version` 기준으로 정한다.

- DevezCode의 xterm 6은 기본 Unicode 6 폭표를 사용해 `🐾` 같은 현대 이모지를 1셀로 두지만 `unicode-width` 17은 2셀로 계산한다. 내부 ConPTY가 명시한 `DEVEZCODE_TERM_WIDTH_PROFILE=xterm6-unicode6`에서만 xterm 6 폭표를 렌더러 전체에 적용하고, 독립·외부 터미널은 현대 폭표를 유지한다. 컴포저만 보정하면 일반 출력·줄바꿈·선택 영역이 다시 어긋난다.

- ConPTY 입력 변환은 자판 대응(VkKeyScan)이 없고 문자 분류(C3_ALPHA)도 아니며 전각도 아닌 문자(사용자 영역 U+E000 등)를 Alt 코드 키 이벤트로 합성한다. crossterm은 이를 누름 없이 놓기(Release) 한 번으로 전달하므로, 호스트→dvz 사설 표식 문자는 Release 단독 도착도 그 문자 자체로 처리해야 한다(`src/preedit.rs`). 한글 자모·음절은 C3_ALPHA라 정상 누름/놓기로 온다.

# 신모델 추가 절차 (1.7.14 기준)

Claude 쪽은 Windows에서 SDK 내장 CLI가 기본이고, 설치된 Claude Code가 실제 exe이며 내장 CLI보다 새 버전일 때만 그쪽을 쓴다. 그래서 신모델은 보통 SDK 상향 배포로 반영한다. 아래 순서대로 처리하면 한 번에 끝난다.

1. 확인: `npm view @anthropic-ai/claude-agent-sdk version`으로 최신 SDK를 보고, `npm/`에서 `npm i` 뒤 bridge에 `{"id":1,"method":"model/list","params":{"cwd":"C:/..."}}`를 stdin으로 보내 신모델이 목록에 오는지 본다(응답까지 20~30초).
2. SDK 고정 버전 갱신: `npm/package.json`의 `@anthropic-ai/claude-agent-sdk`를 올리고 `npm install`로 lock을 맞춘다. `src/claude.rs`의 `bridge_enables_latest_claude_task_and_interrupt_contracts` 테스트가 버전 문자열 두 곳을 고정하므로 같이 고친다.
3. 예비 목록: `src/claude.rs` `model_catalog()`에 신모델을 맨 앞에 넣고 `model_catalog_uses_existing_model_shape` 테스트의 개수·id·표시명을 갱신한다. 시작 시 실시간 조회가 실패하면 이 목록이 보이므로 빠뜨리면 사용자가 신모델을 못 찾는다.
4. 요율: `src/pricing.rs`는 모델 id 부분 일치라 같은 계열 요율이 자동 적용된다. 가격이 다르면 더 구체적인 슬러그를 앞에 추가하고, cache 요율처럼 계열과 다른 값은 `estimate_usd`에서 별도 분기한다(Fable 5.1 cache 읽기 0.025배 사례).
5. 계열명이 새로 생기면(fable/opus/sonnet/haiku 외) bridge `familyCapabilities`·`compactClaudeModelName` 정규식, `state.rs` `matches_query` 별칭, `renderer.rs` `chrome_model_tone` 색상을 함께 추가한다. 카탈로그에서 빠진 구모델 세션은 재개 시 같은 계열 최신으로 자동 이동하므로 계열명이 빠지면 Opus로 떨어진다.
6. 검증: `cargo test`, `node npm/bridge/claude-agent-sdk-bridge.mjs --self-test`(`opening notice order` 실패는 기존 환경 문제), 1번의 model/list 재확인.
7. 배포: `Cargo.toml` 버전 상향 → 실행 중 `dvz.exe`는 이름을 바꿔 치우고 `cargo build --release` → `git pull` → 커밋·push → Bash 도구로 `node scripts/release-npm.mjs --publish --skip-build`.
8. 안내: 사용자는 `npm i -g devez-vibe` 뒤 dvz를 완전히 재시작해야 하고, 기존 세션은 자기 모델을 유지하므로 `/model`에서 바꿔야 한다.

Codex 쪽 주의점

- dvz는 PATH의 `codex`를 그대로 실행하므로 `npm i -g @openai/codex`만으로 신모델이 바로 나타난다. dvz 배포는 필요 없다.
- 다만 비용 표시는 `src/pricing.rs`에 슬러그를 넣어야 한다. `gpt-5.6-luna`처럼 특수 tier는 일반 `gpt-5.6`보다 앞에 두지 않으면 일반 요율로 새어 나간다.
- `/model sol` 같은 별칭은 `state.rs` `matches_query`에, 색상은 `renderer.rs` 톤 매핑에 추가한다.
- Codex의 compact·재개는 rollout 전체에서 복원되므로 모델 변경과 무관하게 손댈 것이 없다.
- Codex가 기존 도구를 선택 활성으로 돌리면 dvz 기능이 조용히 사라진다. 0.152에서 `tools.update_plan.enabled` 기본값이 false가 되어 계획 카드가 비었고, app-server 실행 인자로 켜서 복구했다(1.7.35, Codex 0.153.1 · SDK 0.3.260 기준). 릴리스 노트는 New Features뿐 아니라 Chores 항목까지 확인할 것.
